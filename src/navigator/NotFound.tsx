import { Box, Typography } from '@material-ui/core';

export const NotFound = () => {
  return (
    <Box>
      <Typography>Ops, parece que não encontramos a sua pagina</Typography>
    </Box>
  );
};
